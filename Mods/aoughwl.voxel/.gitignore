# The two tables `tools/mcindex.ps1` reduces out of Mojang's own generated
# reports, from the player's own jar, on the player's own machine. They are
# theirs, they are version specific, and they never go near this repository -
# the same rule `aoughwl.minecraft/.gitignore` states about `Imported/`
# and for the same reason.
Imported/
